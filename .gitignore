# deps / build
node_modules/
dist/
.astro/
.wrangler/

# synced at build time from the OSS repo (the-efficacious/commandsuite docs/)
# — build input, not source. See scripts/sync-oss-docs.mjs.
# No trailing slash: this path is a symlink in LOCAL mode, and a
# directory-only pattern (trailing slash) would not match it.
src/content/docs

# env / editor
.env
.env.*
.DS_Store
